var stream1[];
var stream2;[];
var line[];
line[] = readLine;

while(line != null) {
    var a = line[0]
    var b = readInt(1);
    stream1.push(a);
    stream2.push(b);

    line[] = readLine;
    var c = line[0];
    var d = line[1];
    stream1.push(c);
    stream2.push(d);

    print(stream1.pop())
    print(stream1.pop())
    print(stream2.pop())
    line[] = readLine;
}
