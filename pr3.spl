var stream1 = [];
var stream2 = [];
var left;
var right;
var line = readLine;

print(0);

while(line.length() != 0) {
    
    stream1.push(line[0]);
    
    line = readLine;
  
    stream1.push(line[0]);
    stream2.push(line[1]);

    left = stream1.deQ();
    right = stream2.deQ();

    print(right);
    print(left);

    line = readLine;