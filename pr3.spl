var stream1 = [];
var stream2 = [];

print(0);

var line = readLine;

while(line.length() != 0) {
    var a = line[0];
    stream1.push(a);

    var line = readLine;
    var a = stream1.push(a);
    var b = line[1]
    
    print(stream1.pop());
    print(b);

    var line = readLine;
}
