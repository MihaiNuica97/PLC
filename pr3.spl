var stream1[];
var stream2[];

print(0);

line[] = readLine;

while(line != null) {
    var a = line[0];
    stream1.push(a);

    line[] = readLine;
    var a = stream1.push(a);
    var b = line[1]
    
    print(stream1.pop());
    print(b);

    line[] = readLine;
}
