var line = readLine;
var a;
var b;
var c;

while(line.length() != 0) 
{
    a = line[0]; 
    line = readLine;
    b = line[0];
    line = readLine;
    c = line[0];

    if (line.length() != 0) {
        print(c);
        print(2*b);
        print((3*a) - 1); 
    }
    
    line = readLine;
}